// src/pages/Community/index.jsx
import WorkInProgress from '@/components/WorkInProgress';

/**
 * @module Community
 * @category Pages
 * 
 * @description
 * The `Community` component renders a `WorkInProgress` component 
 * with the title "Communauté", indicating that the page is under development.
 * 
 * @returns {JSX.Element} The rendered `Community` page.
 */
const Community = () => {
  return <WorkInProgress title="Communauté" />;
};

export default Community;
